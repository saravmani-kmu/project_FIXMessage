Create FIX Message parser in .net 8 (c#)
It is for my personal use to parse fix messages.

Create it as desk top app. use quick fix package

user can able to copy paste the messages in text box and click on parse button. show the result in grid view. make it simple UI, as it is for personal use.

once copy pasted it should show a grid with parsed message

Parse the messages,
it may have repeated groups. handle it correctly.
 
Also i will configure dictionary file also to tell aboute custom tags

this parser should handle Fidessa messages.. FIX version 4.2